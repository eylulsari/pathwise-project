import { AppHeader } from '../components/AppHeader';

/** Phase 7 fills this in (buddies, check-ins, community routes, forum). */
export default function Social() {
  return (
    <div className="flex min-h-screen flex-col">
      <AppHeader />
      <div className="flex flex-1 items-center justify-center text-cream/50">
        Social & Travel Buddies — coming in Phase 7.
      </div>
    </div>
  );
}
