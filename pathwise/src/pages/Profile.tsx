import { AppHeader } from '../components/AppHeader';

/** Phase 7 fills this in (past trips, Istanbul passport badges, visited spots). */
export default function Profile() {
  return (
    <div className="flex min-h-screen flex-col">
      <AppHeader />
      <div className="flex flex-1 items-center justify-center text-cream/50">
        Profile & Istanbul Passport — coming in Phase 7.
      </div>
    </div>
  );
}
